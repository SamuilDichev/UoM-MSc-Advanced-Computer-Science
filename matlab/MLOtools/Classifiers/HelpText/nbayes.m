% MLOTOOLS Nbayes
% Naive Bayes with categorical features.
% Arguments: None
%
% Trained by maximum likelihood.
%
% a = nbayes();             %create tree classifier.
% a.train(data, labels);    %train it
% acc = a.test(data,labels).acc();   %get the results
%
